1) What is jQuery?

- jQuery is a open source minified javascript library created for simplified javascript operations,we can use jQuery 
to quickly code a set of diffrent commands,that could take a much longer time if we use HTML code.

2)  How to Apply CSS Using JQuery, How to Add Class and Remove Class in Jquery ,jQuery Animation?

- "jQuery css()" method is used to apply css() using JQUERY.

- using "jQuery() removeClass" we can remove one or more than one class simultaneously.

- similary using "jQuery addClass" we can add one or more than one class.

- The animate () method performs a custom animation of a set of CSS properties. This method changes an element from      
one state to another with CSS styles. 


3)  How to create slider with animation?


- Create the HTML with a container div and the slider div.

In the CSS, set the slider to be offscreen by setting it with a negative left (eg -100px).

We then trigger the slide by using transition CSS property and set the left to be 0px;


4) • Event bubbling tickling example?

- Event Bubbling is a concept in the DOM (Document Object Model).

 It happens when an element receives an event, and that event bubbles up (or you can say is transmitted or 

propagated) to its parent and ancestor elements in the DOM tree until it gets to the root element.
